package pkg;

import com.example.tutorial.Outer;

public class OptionalRepeated {
    public static void main(String[] args) {
        Outer.Request3.Builder builder = Outer.Request3.newBuilder();
        System.out.println(builder);
    }
}
